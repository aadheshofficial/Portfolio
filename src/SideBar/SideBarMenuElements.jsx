import React from 'react'

const SideBarMenuElements = ({MenuElements}) => {
  return (
        <li className="side-bar-menu-elements">
            {MenuElements}
        </li>
  )
}

export default SideBarMenuElements
